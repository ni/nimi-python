# Tox (http://tox.testrun.org/) is a tool for running tests
# in multiple virtualenvs. This configuration file will run the
# test suite on all supported python versions. To use it, "pip install tox"
# and then run "tox -c tox-system_tests.ini" from the driver directory. (generated/niscope)
[tox]
envlist = py{310,311,312,313,314}-niscope-wheel_dep,py{310,311,312,313,314}-niscope-system_tests, py314-niscope-coverage
skip_missing_interpreters=True
ignore_basepython_conflict=True
# We put the .tox directory outside of the Jenkins workspace so that it isn't wiped with the rest of the repo
toxworkdir = ../../../.tox

[testenv]
description =
    niscope-wheel_dep: Build the nitclk wheel because we use it in niscope tests
    niscope-system_tests: Run niscope system tests (requires NI-SCOPE runtime to be installed)
    niscope-coverage: Prepare coverage report for upload to codecov.io  # upload handled by GitHub Actions

changedir =
    niscope-wheel_dep: ../nitclk
    niscope-system_tests: .
    niscope-coverage: .

setenv =
    # Unbuffered so the native glibc/teardown output is not reordered or lost when a process aborts.
    niscope-system_tests: PYTHONUNBUFFERED = 1
    # MALLOC_CHECK_=3: glibc heap corruption detection.
    # On RHEL 9 (glibc 2.34+), aborts with a backtrace on the first heap error,
    # giving a cleaner stack trace than a random SIGSEGV later.
    # Value 3 = print message + abort (2 = print only, 1 = abort only).
    niscope-system_tests: MALLOC_CHECK_=3
    # COREDUMP_FILTER=0xff: include all memory regions (anonymous, file-backed,
    # shared, hugetlb) in the core dump so the C++ vtable and heap state are
    # fully captured for post-mortem analysis with gdb.
    niscope-system_tests: COREDUMP_FILTER=0xff

commands =
    niscope-wheel_dep: python -m build --wheel

    # --disable-pip-version-check prevents pip from telling us we need to upgrade pip, since we are doing that now
    niscope-system_tests: python -m pip install --disable-pip-version-check --upgrade pip
    niscope-system_tests: python ../../tools/install_local_wheel.py --driver nitclk --start-path ../..
    niscope-system_tests: python -c "import niscope; niscope.print_diagnostic_information()"
    # -X faulthandler: dump Python tracebacks on fatal signals. python -v: trace interpreter finalization (# cleanup/# destroy) to see which module unload precedes a -6 abort. pytest -s: don't capture, so native teardown output is inline. -rA: full short test summary.
    niscope-system_tests: python -X faulthandler -v -m coverage run --rcfile=../../tools/coverage_system_tests.rc --source niscope --parallel-mode -m pytest ../../src/niscope/examples -s -rA --junitxml=../junit/junit-niscope-{envname}-examples-{env:BITNESS:64}.xml {posargs}
    niscope-system_tests: python -X faulthandler -v -m coverage run --rcfile=../../tools/coverage_system_tests.rc --source niscope --parallel-mode -m pytest ../../src/niscope/system_tests -c tox-system_tests.ini -s -rA --junitxml=../junit/junit-niscope-{envname}-{env:BITNESS:64}.xml --durations=5 {posargs}

    niscope-coverage: coverage combine --rcfile=../../tools/coverage_system_tests.rc ./
    # Create the report to upload
    niscope-coverage: coverage xml -i --rcfile=../../tools/coverage_system_tests.rc
    # Display the coverage results
    niscope-coverage: coverage report --rcfile=../../tools/coverage_system_tests.rc

deps =
    niscope-wheel_dep: build

    niscope-system_tests: pytest
    niscope-system_tests: coverage
    niscope-system_tests: numpy
    niscope-system_tests: hightime
    niscope-system_tests: fasteners
    niscope-system_tests: pytest-json
    niscope-system_tests: .[grpc]

    niscope-coverage: coverage

depends =
    niscope-coverage: py{310,311,312,313,314}-niscope-system_tests
    niscope-system_tests: py{310,311,312,313,314}-niscope-wheel_dep,

passenv =
    GIT_BRANCH
    GIT_COMMIT
    BUILD_URL
    BRANCH_NAME
    JENKINS_URL
    BUILD_NUMBER

[pytest]
addopts = --verbose
filterwarnings =
   error::pytest.PytestUnhandledThreadExceptionWarning
norecursedirs = .* build dist CVS _darcs {arch} *.egg venv
junit_suite_name = nimi-python
junit_family = xunit1
